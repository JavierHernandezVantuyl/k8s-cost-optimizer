apiVersion: v1
kind: Service
metadata:
  name: optimizer-dashboard
  namespace: cost-optimizer
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app.kubernetes.io/component: dashboard
