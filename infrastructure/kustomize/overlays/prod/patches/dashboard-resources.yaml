apiVersion: apps/v1
kind: Deployment
metadata:
  name: optimizer-dashboard
spec:
  template:
    spec:
      containers:
        - name: dashboard
          resources:
            limits:
              cpu: 1000m
              memory: 1Gi
            requests:
              cpu: 200m
              memory: 256Mi
