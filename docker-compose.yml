version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: k8s-optimizer-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-k8s_optimizer}
      POSTGRES_USER: ${POSTGRES_USER:-optimizer}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-optimizer_dev_pass}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-optimizer}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - optimizer-network

  redis:
    image: redis:7-alpine
    container_name: k8s-optimizer-redis
    command: redis-server --appendonly yes
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - optimizer-network

  minio:
    image: minio/minio:RELEASE.2024-01-16T16-07-38Z
    container_name: k8s-optimizer-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin123}
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - optimizer-network

  prometheus:
    image: prom/prometheus:v2.48.1
    container_name: k8s-optimizer-prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--web.enable-lifecycle'
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - optimizer-network
    depends_on:
      - postgres

  grafana:
    image: grafana/grafana:10.2.3
    container_name: k8s-optimizer-grafana
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin123}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_SERVER_ROOT_URL: http://localhost:3000
      GF_INSTALL_PLUGINS: ""
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./config/grafana/datasources.yml:/etc/grafana/provisioning/datasources/datasources.yml:ro
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - optimizer-network
    depends_on:
      - prometheus

  aws-pricing-api:
    build:
      context: ./services/aws-pricing-api
      dockerfile: Dockerfile
    container_name: k8s-optimizer-aws-pricing
    ports:
      - "5001:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - optimizer-network

  gcp-pricing-api:
    build:
      context: ./services/gcp-pricing-api
      dockerfile: Dockerfile
    container_name: k8s-optimizer-gcp-pricing
    ports:
      - "5002:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - optimizer-network

  azure-pricing-api:
    build:
      context: ./services/azure-pricing-api
      dockerfile: Dockerfile
    container_name: k8s-optimizer-azure-pricing
    ports:
      - "5003:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - optimizer-network

  metrics-generator:
    build:
      context: ./services/metrics-generator
      dockerfile: Dockerfile
    container_name: k8s-optimizer-metrics-generator
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB:-k8s_optimizer}
      POSTGRES_USER: ${POSTGRES_USER:-optimizer}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-optimizer_dev_pass}
      REDIS_HOST: redis
      REDIS_PORT: 6379
    ports:
      - "8001:8001"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - optimizer-network

  optimizer-api:
    build:
      context: ./services/optimizer-api
      dockerfile: Dockerfile
    container_name: k8s-optimizer-api
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB:-k8s_optimizer}
      POSTGRES_USER: ${POSTGRES_USER:-optimizer}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-optimizer_dev_pass}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      AWS_PRICING_URL: http://aws-pricing-api:8000
      GCP_PRICING_URL: http://gcp-pricing-api:8000
      AZURE_PRICING_URL: http://azure-pricing-api:8000
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      aws-pricing-api:
        condition: service_healthy
      gcp-pricing-api:
        condition: service_healthy
      azure-pricing-api:
        condition: service_healthy
    networks:
      - optimizer-network

networks:
  optimizer-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  minio_data:
  prometheus_data:
  grafana_data:
